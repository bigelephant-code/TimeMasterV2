# 更新记录

本项目遵循语义化版本编号。`0.1.4` 是基于 source-equivalent 重建工程定版的首个 Windows x64 正式版本；它不应被描述为原始 0.1.3 二进制的逐字节复现。

## [未发布]

### 新增

- 增加默认关闭的 OpenClaw QQ Bot 远程提醒：QQ Bot 可作为主远程通道；时间大师运行时会独立尝试 Windows 本地通知作为兜底。
- 增加 loopback-only `/hooks/agent` 配置、QQ 私聊/群/频道目标、可选账号 ID、连接检查和测试提醒能力。
- 增加有界远程提醒队列、稳定事件标识、状态分类和退避重试元数据。
- 增加默认关闭的 OpenClaw AI 任务教练：可把模糊待办拆成下一步、问题、材料、行动步骤、建议入口、注意事项和后续建议，并保存步骤完成状态。
- 增加“AI 安排今天”：模型只给出顺序与预计时长，本地确定性排程器负责避开固定时间、午休与缓冲，生成需确认的排程草案。
- 增加整批应用、来源签名冲突保护和精确撤销；任一待办变化时应用或撤销均零部分写入。

### 安全与隐私

- 时间大师保存的 Hook token 副本由 Electron `safeStorage` 单独加密，不进入普通 `settings.json`、`data.json`、保存后的 renderer 配置或投递队列；密码框中的临时输入会在保存后清空且不会回显。OpenClaw 侧仍需在其配置或运行环境中持有同一 token，并作为独立秘密边界保护。
- 默认载荷只包含提醒标题和日期/时间；备注仅在用户明确选择时附带。普通待办、专注、目标和费用数据仍本地优先。
- 明确投递语义：`/hooks/agent` 返回 200 只表示 OpenClaw 已受理，不代表 QQ 已送达；QQ 主动消息可能受目标、账号、最近交互窗口和平台政策限制。
- AI Gateway Token 与提醒 Hook token 分键保存且不得复用；Renderer 无法读回明文，AI 端点仅接受精确 loopback `/v1/responses`。
- AI 备注共享默认关闭；费用、专注记录、目标、延期/完成历史和整个数据文件不进入 AI 请求。模型返回只作为结构化草案，HTTPS 建议链接需用户主动点击。

### 验证

- 增加 AI 配置、数据最小化、结构化函数、响应体上限、确定性排程、容量不足、冲突零写、撤销和状态保留测试。
- 扩展 IPC 安全合同，覆盖全部 54 个 preload 请求通道及 AI 主窗口权限、密钥隔离和外链校验。

## [0.1.11] - 2026-08-13

### 调整

- 桌面小组件“专注办公”更名为“专注时间”，并以包含表盘、顶部按键和倒计时指针的秒表图标替换小时钟图标。
- 待机卡片重新整理为专注名称、本轮计划、今日累计和开始操作三个区域；开始后翻转为深色专注面，并以大号红色字体突出倒计时。

## [0.1.10] - 2026-08-13

### 新增

- 月历底部新增“本月延期”和“本月完成”汇总，点击后展开对应任务明细。
- 延期明细显示原日期、任务标题、清单和顺延目标日期；完成明细显示任务日期、标题、清单和完成时间。
- 重复待办完成并进入下一周期前会留下完成历史，保证月度完成统计不漏项。

### 调整

- 移除费用页顶部重复的“费用分类”按钮，只保留“记一笔”区域内语义更明确的“编辑费用分类”入口。
- 桌面小组件的专注卡片在开始后翻转为高对比大字号倒计时，暂停/继续留在计时面，取消后直接翻回初始面。
- 主界面和桌面小组件中的费用分类按钮改为按文字宽度自适应，“货款”不再独占整行宽度。

### 验证

- 新增完成历史的规范化、去重、重复待办日期推进和月度明细合同测试。
- 打包视觉烟测新增月度延期、月度完成和桌面专注翻转计时状态，并验证取消后返回初始面。

## [0.1.9] - 2026-08-13

### 新增

- 未完成待办自动顺延前，会在原日期保存状态为“未完成”的只读顺延记录，包括原日期、目标日期、当时标题、清单和记录时间。
- 月历统计会把顺延记录计入原日期的未完成数量；点击原日期可在右侧查看顺延去向。

### 验证

- 新增顺延记录的规范化、去重、连续多日顺延和日历集成测试。
- 打包应用视觉烟测新增“顺延历史”状态，覆盖原日期标记与详情排版。

## [0.1.8] - 2026-08-13

### 改进

- 月历任务摘要改为两行状态：橙色显示未完成数量，绿色显示已完成数量，数字右对齐并限制在日期格边界内。
- 产品对外名称由“时间大师 V2”统一为“时间大师”，覆盖窗口、侧栏、托盘、设置、快捷方式、可执行文件、安装包和 Excel 元数据。

### 兼容性

- 保留 `com.timemaster.v2`、`timemaster-v2` 和 `%APPDATA%\timemaster-v2\` 内部身份，不迁移或重置用户数据；历史 0.1.3 安装包名称和恢复证据保持不变。

## [0.1.7] - 2026-08-13

### 改进

- 月历日期格不再重复显示待办标题和时间，任务详情统一由右侧日期面板承载。
- 有待办的日期格改为显示精确待办数量与六段密度刻度；五项及以上使用更明显的高密度状态，兼顾信息提示和月历可读性。

### 验证

- 使用同一天八项虚构待办完成打包应用视觉验收，确认日期格无标题压缩且右侧详情完整保留。

## [0.1.6] - 2026-08-13

### 修复

- 桌面小组件误点费用分类后，现在可点击输入区以外的空白处或按 `Esc` 取消快捷记账。
- 取消快捷记账会清空尚未提交的金额与备注，不会新增费用记录。

### 验证

- 增加小组件快捷记账打开、点击空白处关闭的打包应用交互烟雾测试，并保留打开状态截图用于视觉验收。

## [0.1.5] - 2026-08-13

### Changed

- 重做主窗口视觉系统：统一标题栏、侧栏导航、工具栏、按钮、输入框、空状态和弹窗层级，并兼顾深浅主题与减少动态效果偏好。
- 重新设计日历月视图、日程侧栏、全部待办和四象限，使日期、任务状态和决策分区更容易快速扫描。
- 将“编辑费用分类”入口放到“记一笔”的实际分类按钮旁；新增、修改名称、删除和恢复均直接作用于该组按钮。
- 将用户界面的“停用”改为更直观的“删除”；删除分类按钮仍保留稳定类别编号和历史账目，可随时恢复。
- 扩展打包应用视觉烟测，覆盖日历、待办、四象限、设置、费用总览、费用分类、紧凑窗口、审计视图和桌面小组件。

## [0.1.4] - 2026-08-12

### Release note

- 定版 Windows x64 NSIS 正式安装包 `TimeMasterV2-Setup-0.1.4.exe`；该版本未经过 Authenticode 签名，可能触发 Windows SmartScreen。
- 本变更记录描述版本状态，不表示安装包已经上传到 GitHub Release；实际下载内容与 SHA-256 以发布渠道中的真实附件为准。

### Added

- 添加从 `src/` 到 `out/` 的 `electron-vite` 构建链和构建产物验证。
- 添加 0.1.3 黄金参考哈希、preload/IPC 契约和窗口安全选项测试。
- 为主窗口和小组件启用 Electron renderer sandbox，并增加使用隔离数据目录的打包应用 smoke test。
- 修复恢复出的日历 bundle 中两处错误的后缀边界判断，并将开发环境最低版本与 Electron 43 对齐到 Node.js 22.12。
- 将 0.1.4 开发和打包运行时升级到 Electron 43.3.0；恢复出的 renderer 仍如实保留其内嵌 Vue 3.5.40。
- 将任务时间规范化与旧 `time` 字段回退逻辑提取为可直接单测的主进程模块，并添加表驱动边界测试。
- 添加隐私说明、威胁模型、支持指南、第三方完整许可证和使用虚构数据的脱敏截图。
- 费用台账支持每本台账独立新增、重命名、停用和恢复期间费用类别；货款单列仍保持固定口径。
- 添加数据模型 v3→v4 的费用类别迁移，以及稳定类别编号、遗留分类保留和类别边界测试。

### Changed

- 将从项目所有者 0.1.3 产物重建的 source-equivalent 实现设为当前可编辑构建输入。
- 将不可变 0.1.3 运行时保留在 `runtime/`，把 LiteCal 0.1.0 历史源码移至 `legacy/litecal-0.1.0/`。
- 显式启用 renderer 上下文隔离、关闭 Node 集成、限制新窗口/外部导航并校验 IPC 来源。
- 在生产构建中移除仅开发环境需要的 WebSocket CSP 来源。
- 改进费用类别管理界面；停用类别不再出现在新记账选择器中，但历史明细、汇总和 Excel 导出仍保留并计入。
- Excel 对账改为按稳定类别编号汇总，并根据实际类别数量动态生成分类行、公式和对账区域。

### Recovery note

- 当前 `src/` 不是丢失的原始源码。原始安装包没有 source map 或 Vue 单文件组件，当前构建也不宣称与 0.1.3 逐字节一致。

## [0.1.3] - 2026-08-03

- 发布项目所有者制作的最终 Windows 安装包 `TimeMasterV2-Setup-0.1.3.exe`。
- 增加自定义专注计时与每日专注记录。
- 增加目标、周期累计和费用台账三类主任务。
- 增加费用明细补录、按日期管理和 Excel 对账导出。
- 增加 Open-Meteo 天气、地理位置选择与缓存。
- 扩展桌面小组件、任务起止时间、预计用时和状态提示。
- 将应用身份独立为 `timemaster-v2` / `com.timemaster.v2`。
- 发布归档说明：该原始安装包未经过 Authenticode 签名；SHA-256 为 `8EEF4DB7BDF2F8BA4911C97E9189232DA9D7D362034822E51A6D03DB333DE9E4`。

## [0.1.0] - 2026-07-26

- 发布 LiteCal 初始模块化源码。
- 支持日历、待办、四象限、提醒、计时、长期目标和桌面小组件。
