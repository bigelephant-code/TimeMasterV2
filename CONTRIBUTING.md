# 参与贡献

感谢你帮助改进时间大师 V2。

## 开始之前

1. 先搜索现有 Issues，避免重复。
2. 较大的功能或数据格式变化，请先开 Issue 讨论设计和迁移方案。
3. 不要提交真实用户数据、日志、账号、密钥或本机绝对路径。

## 本地验证

```powershell
npm ci
npm run check
npm start
```

提交前至少确认：

- 主窗口和桌面小组件能够打开。
- 新建、修改和完成待办后数据可以持久化。
- 专注、主任务或台账的改动不会破坏旧数据。
- `npm run check` 通过。
- `git diff --check` 无空白错误。

## 源码现状

- `runtime/` 是 0.1.3 当前运行版本，来源和限制记录在 `docs/RECOVERY.md`。
- `src/` 是 LiteCal 0.1.0 的历史模块化源码，仅用于迁移参考。
- 新功能不要只改安装包或 `release/`；必须同步提交可审查源码和验证方法。
- 对 `runtime/` 做较大改动时，优先把相关逻辑拆回清晰模块，并保持数据兼容。

## Pull Request

- 每个 PR 聚焦一个问题。
- 标题说明用户可感知的结果。
- 描述中写清修改原因、数据兼容影响和验证步骤。
- UI 改动请附截图或短视频。
