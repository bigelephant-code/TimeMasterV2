# 安全政策

## 支持范围

当前维护版本为 0.1.3。更早的 LiteCal V1 源码仅作为迁移参考。

## 报告漏洞

请优先使用 GitHub 的 Private vulnerability reporting / Security Advisory 私下报告漏洞。不要在公开 Issue 中粘贴利用代码、真实用户数据、访问令牌或其他敏感信息。

如果私密报告入口暂不可用，可以创建一个不含敏感细节的 Issue，请维护者开启私密沟通。报告中建议包含：

- 受影响版本和 Windows 版本；
- 可重复的最小步骤；
- 预期影响；
- 建议修复方向（如有）。

核心数据保存在本机 `%APPDATA%\timemaster-v2\`。任何涉及该目录、IPC 白名单、外部链接、天气请求或安装/卸载数据处理的改动都应按安全敏感变更审查。
